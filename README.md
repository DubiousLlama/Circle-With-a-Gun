# Circle-With-a-Gun
 
## Platform debugging

To debug on desktop, make sure to return `PlatformType.Desktop` in `Assets/Platform.cs` under the `Editor` cases. For mobile, return `PlatformType.Mobile`, and use the Unity Remote app.
